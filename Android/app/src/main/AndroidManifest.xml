<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.markopavicic.orwma_projekt">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.ORWMA_Projekt"
        android:windowSoftInputMode="adjustResize">
        <activity android:name=".TeamManager"></activity>
        <activity android:name=".ChooseTeam" />
        <activity android:name=".ChoosePlayers" />
        <activity android:name=".ViewPagerActivity" />
        <activity android:name=".WelcomeSplash" />
        <activity android:name=".RegisterUser" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>